{
  "extends": "./tsconfig",
  "include": ["src", "test"],
  "compilerOptions": {
    // this config only ever runs with --noEmit, so the inherited rootDir of
    // "src" is just a constraint that would reject TypeScript tests under test/
    "rootDir": "."
  }
}
